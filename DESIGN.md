# 주크박스 화면 디자인

## 1. 방향
곡을 찾고 신청하는 데 집중해요. 기존 와인색 포인트와 앨범 이미지를
유지하고, 겹친 유리 효과와 넓은 그림자 대신 단정한 불투명 표면을 써요.
게스트 화면은 곡 신청에, 관리자 화면은 매장 운영에 집중해요.

## 2. 색
기존 `--guest-*` 변수를 사용해요.
밝은 테마는 흰 표면, `#27242b` 본문, `#66646e` 보조 문구,
`#bf384b` 강조색이에요. 어두운 테마는 `#202126` 표면,
`#f6f4f1` 본문, `#aaaab2` 보조 문구, `#f35b63` 강조색을 유지해요.
구분선은 본문색 10%, 상태 배경은 강조색 14%예요.
게스트의 브라우저 `theme-color`와 문서 배경은 밝을 때 `#ffffff`,
어두울 때 `#000000`으로 맞춰요. 초기 쿠키 복원, 직접 선택,
시스템 테마 변경 모두 같은 색을 적용해요.

## 3. 글자
기존 시스템 한글 폰트 스택을 유지해요. 검색 제목은 1.45~1.75rem,
곡 제목은 기존 0.95rem, 보조 문구는 0.78~0.88rem이에요.
상세 곡 제목은 1.3~1.8rem이며 긴 제목은 줄바꿈해요.
목록의 제목과 가수는 한 줄 말줄임을 유지해요.

## 4. 간격과 배치
본문 최대 너비는 640px, 좌우 여백은 20px(420px 이하는 16px)이에요.
상단은 16px 위, 20px 아래 여백으로 시작해요.
문서가 검색 목록 스크롤을 담당하고, 검색창만 상단에 고정해요.
미니 플레이어는 본문과 같은 좌우 여백에 맞추고 68px 높이,
하단 8px와 safe-area 여백을 둬요. 목록 끝에는 104px와 safe-area를
확보해 마지막 곡을 플레이어 위로 올릴 수 있게 해요.
상세 패널은 자체 스크롤을 유지하며 헤더 배경이 패널 전체 폭을 덮어요.

## 5. 기존 컴포넌트
- TopBar: 브랜드, 테마 선택 메뉴, 테이블 표시. 테마 세 가지를 유지해요.
  선택 메뉴는 44px 터치 높이와 접근 가능한 이름을 가져요.
- SearchPanel: 제목, 검색창, 상태 안내, 곡 목록 순서예요.
  장식용 소제목과 중복 설명은 빼고 입력 placeholder로 안내해요.
  검색 중, 결과 없음, 신청 중, 신청 불가 상태는 유지해요.
- Hint: 신청 제한 상태만 보조 문구로 표시해요.
  신청 중지는 기존 배너 한 곳에서만 알려줘요.
- NowPlayingArea: 미니 플레이어와 상세 패널을 유지해요.
  미니 플레이어는 불투명 배경, 앨범 48px, 약한 그림자예요.
  상세 패널과 헤더는 같은 배경이며 앨범 그림자는 없어요.
- 신청 버튼, 토스트, 대기열: 기존 동작과 상태를 유지해요.

## 6. 동작
기존 240ms 버튼 피드백과 480ms 패널 전환을 유지해요.
reduced-motion 처리를 유지해요.
테마 변경은 기존 쿠키 저장 및 시스템 테마 연동을 그대로 사용해요.
상세 패널은 상단 76px 너비, 44px 높이의 핸들 버튼으로 닫아요.
핸들을 탭하면 아래로 닫히고, 드래그 중에는 손가락을 그대로 따라가요.
아래로 100px 이상 끌어 놓으면 닫히며 짧은 드래그는 원위치로 돌아와요.
핸들 외 본문은 계속 스크롤할 수 있어요. Pointer Events의 capture로
핸들 밖에서 놓아도 제스처를 마무리하고, 취소된 제스처는 원위치로 돌아와요.
동작 참고: https://beui.dev/r/bottom-sheet/raw 의 핸들 전용 드래그와
고정 body 스크롤 잠금. 기존 CSS 전환을 사용하며 의존성은 추가하지 않아요.
배경은 패널의 닫힘 전환이 끝날 때까지 고정하고, 닫은 뒤 원래 스크롤
위치를 복원해요. 네이티브 dialog로 초점을 패널 안에 가두고 Escape와
배경 탭으로도 같은 닫기 전환을 실행해요.

## 7. 표면
미니 플레이어와 토스트 그림자는 밝은 테마에서
`0 2px 8px rgba(70, 41, 54, 0.08)`, 어두운 테마에서
`0 2px 8px rgba(0, 0, 0, 0.2)`예요.
입력, 앨범, 상세 패널에는 그림자가 없어요.
미니 플레이어의 반투명 배경, blur, 안쪽 하이라이트는 제거해요.
플레이어 모서리는 16px, 내부 앨범은 8px로 맞춰요.
상세 패널의 기존 28px 상단 모서리는 유지해요.

## 8. 접근성과 검증
키보드 초점 표시, 비활성 상태, 입력 이름, 테마 선택 이름을 유지해요.
375px, 768px, 1280px에서 밝은/어두운 테마, 긴 곡 제목,
검색 결과, 신청 제한, 상세 열기/닫기, 대기열을 확인해요.
Safari의 실제 브라우저 툴바와 safe-area 합성은 데스크탑 에뮬레이션으로
완전히 재현할 수 없으므로 실기기 확인 여부를 결과에 명시해요.

## 9. 관리자 화면
게스트의 와인색 포인트를 이어받고, 운영 정보는 중립색으로 정리해요.
기존 `/admin/songs`, `/admin/qr` 문서와 인증/API 흐름은 유지해요.

- 색: 밝은 배경 `#f5f4f5`, 패널 `#ffffff`, 본문 `#27242b`,
  보조 `#6e6872`, 구분선 `#e5e1e5`, 강조 `#bf384b`와 흰 글자.
  어두운 배경 `#111114`, 패널 `#1b1b20`, 본문 `#f6f3f5`,
  보조 `#aaa5af`, 구분선 `#303037`, 강조 `#ef93a0`와 `#261418` 글자.
  정상 접수는 작은 초록 상태 점, 삭제는 옅은 빨강 배경으로 표시해요.
- 글자: 기존 시스템 폰트. 페이지 제목 1.75rem, 패널 제목 1rem,
  본문 0.875~1rem, 보조 0.75~0.875rem. 숫자는 tabular-nums예요.
- 배치: 최대 1180px, 화면 여백 32px(모바일 16px), 패널 간격 24px,
  패널 내부 24px(모바일 18px). 960px 아래는 한 열이에요.
  공통 상단에 브랜드·실제 라우트 메뉴·접수 상태·테마 선택을 둬요.
  곡 관리의 왼쪽은 재생과 대기열, 오른쪽 360px는 검색과 운영 설정이에요.
  QR 관리는 300px 생성 폼과 테이블 목록으로 나눠요.
- 표면: 불투명 배경과 얇은 중립 구분선, 모서리 패널 16px·입력 10px.
  큰 그림자, blur, 장식용 그라데이션, 이모지 아이콘은 사용하지 않아요.
- 컴포넌트: panel/panel-head, btn/ghost/danger, field/field-label,
  count, empty, note를 두 라우트가 공유해요. 입력과 버튼은 44px 이상이며,
  모든 입력에는 이름, 상태 변경에는 상태 표시, 키보드에는 focus-visible을 둬요.
- 동작: 로그인은 Enter로 제출해요. 검색은 Enter와 검색 버튼을 지원해요.
  대기열 드래그는 유지하고 위/아래 이동 버튼으로 터치·키보드도 지원해요.
  최근 재생은 접을 수 있어요. 접수 스위치, 공지, 곡 수 제한은 별도 그룹이에요.
  QR은 테이블 행 안에서 펼치고 전체 인쇄는 기존 인쇄 포털을 사용해요.
- 스크롤: 문서가 기본 스크롤을 소유해요. 검색 결과만 최대 360px 영역 안에서
  스크롤해요. 새 모션은 추가하지 않고 버튼의 짧은 피드백만 유지해요.
- 검증: 밝은/어두운 테마, 375px·768px·1280px, 로그인 실패·성공,
  검색·추가·순서·삭제·스킵, 접수·공지·제한, 테이블 생성·QR·삭제·인쇄,
  긴 곡/테이블 이름과 빈 상태를 실제 화면에서 확인해요.
